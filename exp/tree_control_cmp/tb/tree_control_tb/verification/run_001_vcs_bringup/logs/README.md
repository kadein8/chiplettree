# Run 001 Logs

This directory will hold all generated logs for `run_001_vcs_bringup`.

The current policy is to keep all logs for this verification round together in
this single directory instead of splitting them into deeper compile/run/result
subdirectories.

Expected future contents include:

1. compile logs
2. simulation run logs
3. simple result-summary text files
