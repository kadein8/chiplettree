# Logs

This directory stores VM-generated `summary`, `result`, `compile.log`, and
`run.log` files for `run_047_vcs_control_chip_third_flush_writeback_closure`.

PASS or FAIL must be judged only after reading:

1. `run_047_summary.txt`
2. the main TB `*_result.txt`
3. the main TB `*_run.log`
4. each regression `*_result.txt`
5. each regression `*_run.log`
