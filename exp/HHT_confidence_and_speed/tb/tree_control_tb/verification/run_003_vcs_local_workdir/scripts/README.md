# Run 003 Scripts

This directory holds the VCS execution scripts for
`run_003_vcs_local_workdir`.

The main change in this round is that VCS build outputs are redirected to a
VM-local work directory while logs remain in the shared project workspace.
