# Run 004 Scripts

This directory holds the VCS execution scripts for `run_004_vcs_agu_stage_b`.

This round keeps the VM-local VCS build-output strategy and extends the target
set with the new AGU Stage B testbench.
