# Scripts

This directory stores the VCS execution scripts for
`run_006_vcs_stage_b_realign`.

Each script should remain scoped to the current Stage A regression plus the
current realigned Stage B verification slice.

The expected usage matches the earlier runs:

1. `cd /home/ICer/first/verification/run_006_vcs_stage_b_realign/scripts`
2. `bash run_all_vcs.sh`

The scripts then write all returned logs and summaries into the sibling
`../logs/` directory.

